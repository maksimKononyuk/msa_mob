import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 80
  },
  searchingText: {
    fontFamily: 'Roboto',
    fontSize: 18,
    padding: 5
  }
})

export default styles
