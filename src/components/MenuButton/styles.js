import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  headerButton: {
    width: 30,
    marginRight: 13
  },
  headerButtonLine: {
    height: 3,
    marginTop: 7
  }
})

export default styles
