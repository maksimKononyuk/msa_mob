import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    alignSelf: 'center',
    marginBottom: 5
  },
  image: {
    width: '100%',
    height: '100%'
  },
  fileContainer: {
    width: '50%',
    height: '50%',
    alignItems: 'center'
  }
})

export default styles
